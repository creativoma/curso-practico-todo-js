// Este archivo index tiene las referencias a todas las clases y de esta manera
// exportamos todoas las clases juntas al index del proyecto

import { Todo } from './todo.class';
import { TodoList } from './todo-list.class';

export{
    Todo,
    TodoList
}